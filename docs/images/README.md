# README screenshots

The panel screenshots embedded in the repo [README](../../README.md).

| File | Shows |
| --- | --- |
| `panel-build.png` | Build tab — the building-blocks palette. |
| `panel-properties.png` | Properties tab — the field editor. |
| `panel-arrow.png` | Arrow toolset — copy/replace fields across a connector. |
| `panel-generate.png` | Generate tab — draft a model from pasted text. |
| `completeness.png` | A reddened arrow captioned with the missing field. |

**Re-capture these whenever the panel UI changes** — they're hand-captured, not
generated by any build step, so nothing keeps them in sync with the code. The
marketplace listing shots in [`branding/screenshots/`](../../branding/screenshots/)
track the same UI and drift the same way.
